#!/usr/bin/env bash

# Kill already running process
_ps=(waybar swayidle lxpolkit)
for _prs in "${_ps[@]}"; do
	if [[ `pidof ${_prs}` ]]; then
		killall -9 ${_prs}
	fi
done

# Idle
hypridle &

# wallpaper 
hyprwall &

# theme
hyprtheme &

# plugins 
hyprctl plugin load $HOME/.config/hypr/split-monitor-workspaces.so &

# polkit 
/usr/libexec/polkit-gnome-authentication-agent-1 &

# Start Apps
ckb-next &
QT_QPA_PLATFORM=xcb insync start & 
thunar --daemon &
foot --server &

#xdg-portal-hyprland &

dbus-update-activation-environment --all &
gnome-keyring-daemon -r -d &

ags -b hypr &
pypr &
emacs --daemon &
