#!/usr/bin/env bash
# [[file:../../Emacs.org::*i3 integration][i3 integration:1]]
if [[ $(xdotool getactivewindow getwindowname) =~ ^emacs(:.*)?@.* ]]; then
    command="(my/emacs-bsp-integration \"$@\")"
    emacsclient -e "$command"
else
    bspc $@
fi
# i3 integration:1 ends here
