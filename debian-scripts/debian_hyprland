#!/bin/bash

cd && apt list --installed | cut -d/ -f1 > pre-cleaned.txt
# Necessary files

sudo rm -rf /etc/apt/sources.list
sudo cp -R $HOME/.dots/debian-scripts/debian.sources /etc/apt/sources.list.d/
sudo cp -R $HOME/.dots/debian-scripts/preferences /etc/apt/
sudo cp -R $HOME/.dots/debian-scripts/01norecommend /etc/apt/apt.conf.d/
sudo cp -R $HOME/.dots/variables.sh /etc/profile.d/

# Installing packages
cd && sudo apt update && sudo apt install zstd curl apt nala

sudo nala update && sudo nala upgrade

# remove wayland-protocols & wayland-utils later because
# you are going to manually build it.

sudo nala install build-essential gcc cmake clang ubuntu-dev-tools make meson wget curl thunar thunar-volman thunar-media-tags-plugin thunar-archive-plugin tumbler git alacritty neovim deluge bat maim python3-pynvim ripgrep tree-sitter-cli fd-find wl-clipboard golang-go zsh zsh-autosuggestions zsh-syntax-highlighting zoxide fzf papirus-icon-theme ranger policykit-1-gnome x11-xkb-utils x11-xserver-utils xournalpp zathura zathura-pdf-poppler pymol pipx rng-tools haveged lynis auditd sysstat acct xserver-xorg-core network-manager qt5-style-kvantum qt6ct qt5ct lsd kitty ncurses-term pipewire pipewire-alsa pipewire-pulse wireplumber xdg-desktop-portal grim slurp wayland-utils wayland-protocols greetd apt-listbugs apt-listchanges  libxcb-util-dev gettext gettext-base xdg-desktop-portal-wlr xdg-desktop-portal-gtk fontconfig hwdata xwayland cpio gnome-keyring  maim systemd-zram-generator xserver-xorg-video-amdgpu nvidia-driver firmware-misc-nonfree wireplumber flatpak  alsa-utils amd64-microcode binutils bluez bluez-alsa-utils ca-certificates cron chrony debianutils fwupd gvfs logrotate plymouth plymouth-themes power-profiles-daemon psmisc xz-utils firmware-linux-free firmware-linux-nonfree firmware-amd-graphics cliphist 

# Development Packages
sudo nala install cmake-extras libcairo2-dev libzip-dev librsvg2-dev libtomlplusplus-dev libspa-0.2-dev libwayland-client++1 libdrm-dev pkg-config ninja-build scdoc libgbm-dev libinih-dev uuid-dev qt6-base-dev qt6-wayland-dev libpipewire-0.3-dev libsdbus-c++-bin libghc-cairo-dev libglvnd-dev libglvnd-core-dev libturbojpeg0-dev libwebp-dev libwlroots-dev libmagic-dev libpango1.0-dev libjpeg-dev libsdbus-c++-dev libegl1-mesa-dev libpam0g-dev libpugixml-dev libegl-dev glslang-tools libinput-bin libinput-dev libxcb-composite0-dev libavutil-dev libavcodec-dev libavformat-dev libxcb-ewmh2 libxcb-ewmh-dev libxcb-present-dev libxcb-icccm4-dev libxcb-render-util0-dev libxcb-xinput-dev libtomlplusplus3 libfontconfig-dev libffi-dev libxml2-dev libxkbregistry-dev libxkbcommon-dev libpixman-1-dev libudev-dev libseat-dev seatd libxcb-dri3-dev libvulkan-dev libvulkan-volk-dev libvkfft-dev libgulkan-dev libgles2 libxcb-res0-dev libliftoff-dev libdisplay-info-dev libsystemd-dev libwayland-dev vulkan-utility-libraries-dev libsecret-1-0 libsecret-1-dev libglib2.0-dev libwayland-client++1 libegl1-mesa-dev libxkbcommon-x11-dev libpam0g-dev libsecret-tools libgtk-layer-shell-dev libgdk3.0-cil-dev libpango1.0-dev librust-gdk-pixbuf-sys-dev libdbusmenu-gtk3-dev libcairo2-dev libcairo-gobject2 libgio3.0-cil-dev librust-glib-sys-dev librust-gobject-sys-dev


# rustup setup
sudo nala install rustup
rustup default stable

# node setup
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash - &&\
sudo nala install nodejs

# spotify
curl -sS https://download.spotify.com/debian/pubkey_6224F9941A8AA6D1.gpg | sudo gpg --dearmor --yes -o /etc/apt/trusted.gpg.d/spotify.gpg
echo "deb http://repository.spotify.com stable non-free" | sudo tee /etc/apt/sources.list.d/spotify.list
sudo nala update && sudo nala install spotify-client

# Installs pacstall & pkgs"
sudo bash -c "$(curl -fsSL https://pacstall.dev/q/install || wget -q https://pacstall.dev/q/install -O -)"

# flatpak setup & pkgs
# sudo flatpak remote-add --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo
# flatpak install flathub org.ferdium.Ferdium 
# flatpak install flathub com.ticktick.TickTick
# flatpak install flathub com.bitwarden.desktop
# flatpak install flathub-beta org.zotero.Zotero

cd && cd $HOME/.dots/debian-scripts 
echo "Installing From Source"
bash ./source_builds/01_hyprutils
bash ./source_builds/02_hyprlang
bash ./source_builds/03_hyprcursor
bash ./source_builds/04_xdg-desktop-portal-hyprland
bash ./source_builds/05_hyprpaper
bash ./source_builds/06_hyprpicker
bash ./source_builds/07_hypridle
bash ./source_builds/08_hyprlock
bash ./source_builds/09_hyprshot
bash ./source_builds/10_hyprwayland-scanner
bash ./source_builds/11_aquamarine
bash ./source_builds/12_wayland-protocols
bash ./source_builds/13_hyprland
bash ./source_builds/14_hyprland-contrib
bash ./source_builds/15_eww
bash ./source_builds/16_lazygit
bash ./source_builds/17_insync

echo "Install Brave"
sudo curl -fsSLo /usr/share/keyrings/brave-browser-archive-keyring.gpg https://brave-browser-apt-release.s3.brave.com/brave-browser-archive-keyring.gpg
echo "deb [signed-by=/usr/share/keyrings/brave-browser-archive-keyring.gpg] https://brave-browser-apt-release.s3.brave.com/ stable main"|sudo tee /etc/apt/sources.list.d/brave-browser-release.list
sudo nala update
sudo nala install brave-browser

echo "Install Google CHrome"
cd Downloads
wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
sudo nala install ./google-chrome-stable_current_amd64.deb
cd 

# pipx pkgs 
pipx install pyprland
pipx install hyprshade

echo "Setting up Dotfiles"
sudo rm -R ~/.config/
cd && mkdir ~/.config
ln -s ~/.dots/.fonts ~/
ln -s ~/.dots/.themes ~/
ln -s ~/.dots/gentoo_setup ~/
ln -s ~/.dots/org ~/
ln -s ~/.dots/.gtkrc-2.0 ~/
ln -s ~/.dots/catppuccin.css ~/
ln -s ~/.dots/.config/hypr ~/.config/
ln -s ~/.dots/.config/eww ~/.config/
ln -s ~/.dots/.config/starship.toml ~/.config
ln -s ~/.dots/.config/Thunar ~/.config/
ln -s ~/.dots/.config/alacritty ~/.config/
ln -s ~/.dots/.config/btop ~/.config/
ln -s ~/.dots/.config/gtk-3.0 ~/.config/
ln -s ~/.dots/.config/gtk-4.0 ~/.config/
ln -s ~/.dots/.config/Kvantum ~/.config/
ln -s ~/.dots/.config/lazygit ~/.config/
ln -s ~/.dots/.config/lsd ~/.config/
ln -s ~/.dots/.config/qalculate ~/.config/
ln -s ~/.dots/.config/qt5ct ~/.config/
ln -s ~/.dots/.config/qt6ct ~/.config/
ln -s ~/.dots/.config/ranger ~/.config/
ln -s ~/.dots/.config/wallpapers ~/.config/
ln -s ~/.dots/.config/zathura ~/.config/
ln -s ~/.dots/.config/zsh ~/.config/
ln -s ~/.dots/.local/bin/hypr ~/.local/bin/


# Set Up Systemd
sudo systemctl set-default graphical.target
systemctl --user enable --now wireplumber.service pipewire-pulse.socket pipewire.socket pipewire-pulse.service pipewire.service pipewire-pulse.socket pipewire.socket pipewire-pulse.service pipewire.service

# Setup zsh
zsh <(curl -s https://raw.githubusercontent.com/zap-zsh/zap/master/install.zsh) --branch release-v1
curl -sS https://starship.rs/install.sh | sh
curl -fsSL https://fnm.vercel.app/install | sh
rm -R ~/.zshrc
ln -s ~/.dots/.zshrc /
chsh -s $(which zsh)

# git setup 
sudo make --directory=/usr/share/doc/git/contrib/credential/libsecret
git config --global credential.helper /usr/share/doc/git/contrib/credential/libsecret/git-credential-libsecret
git config --global user.name "zielOS"
git config --global user.email "ahsanur041@gmail.com"

# lunarvim setup
mkdir ~/.npm-global 
npm config set prefix '~/.npm-global'
export PATH=~/.npm-global/bin:$PATH
bash <(curl -s https://raw.githubusercontent.com/LostNeophyte/lunarvim/master/utils/installer/install.sh)
cd && sudo install -Dm755 $HOME/.local/bin/lvim "/usr/local/bin/lvim"
sudo rm -rf $HOME/.local/bin/lvim
sudo rm -rf $HOME/.config/lvim
ln -s $HOME/.dots/.config/lvim $HOME/.config

echo "End of Hyprland Setup"
